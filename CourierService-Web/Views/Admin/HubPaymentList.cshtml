@model IEnumerable<HubPayment>

@{
    ViewData["Title"] = "Hub Payment List";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<div class="content-page">
    <div class="content">
        <div class="container-fluid">
            <div class="row justify-content-center">
                <div class="col-lg-12">
                    <div class="card-box">
                        <h4 class="header-title text-success">Hub Payment List</h4>
                        <div class="table-responsive">
                            <div class="mb-3">
                                <a asp-action="HubPaymentList" asp-route-showDuePayments="false" class="btn btn-primary">All Payments</a>
                                <a asp-action="HubPaymentList" asp-route-showDuePayments="true" class="btn btn-danger">Due Payments Only</a>
                            </div>
                            <table class="table mb-0">
                                <thead>
                                    <tr>
                                        <th>SL</th>
                                        <th>Hub Name</th>
                                        <th>Total Amount</th>
                                        <th>Amount Received</th>
                                        <th>Due Amount</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @{
                                        int sl = 1;
                                    }
                                    @foreach (var payment in Model)
                                    {
                                        <tr>
                                            <td>@sl</td>
                                            <td>@payment.Hub.Name</td>
                                            <td>@payment.TotalAmount</td>
                                            <td>@payment.AmountReceived</td>
                                            <td>@payment.DueAmount</td>
                                        </tr>
                                        sl++;
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
